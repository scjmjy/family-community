// import { Request, Response, NextFunction } from 'express'
// import querystring from 'querystring'

// export default (req: Request, res: Response, next: NextFunction) => {
//     if (req.method === 'GET') {
//         const ss =
//         req.params = querystring.parse(req.originalUrl, '&', '=') as any
//     }
//     next()
// }
